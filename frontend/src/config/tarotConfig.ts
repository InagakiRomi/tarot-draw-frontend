/** 預設顯示的卡牌數量 */
const defaultCardCount = 5

/** 大阿爾卡納卡牌數量 */
const majorCount = 22

/** 小阿爾卡納卡牌數量 */
const minorCount = 78

/** 塔羅卡背 id */
const tarotBackId = 78

/** 塔羅最小編號 id */
const minTarotCardId = 0

/** 塔羅最大編號 id (大阿爾卡納) */
const maxTarotCardIdMajor = 21

/** 塔羅最大編號 id (小阿爾卡納) */
const maxTarotCardIdMinor = 77

export {
  defaultCardCount,
  majorCount,
  minorCount,
  tarotBackId,
  minTarotCardId,
  maxTarotCardIdMajor,
  maxTarotCardIdMinor,
}
